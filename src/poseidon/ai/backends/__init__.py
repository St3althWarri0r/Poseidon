"""Pluggable LLM backends for the AI layer.

The ``build_backend`` factory and re-exports are added in a later task; for now
this package holds the normalized types and the concrete backends.
"""
from __future__ import annotations
